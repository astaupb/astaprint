[package]
name = "snmp"
version = "0.1.0"
authors = ["Gerrit Pape <papeg@mail.upb.de>"]
edition = "2018"

[dependencies]
netsnmp-sys = "*"
mysql = {path = "../mysql"}
diesel = {version = "*", features = ["mysql", "r2d2"]}
