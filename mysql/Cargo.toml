[package]
name = "mysql"
version = "0.1.0"
authors = ["Gerrit Pape <papeg@mail.upb.de>"]
edition = "2018"

[dependencies]
diesel = {version = "1.3", features = ["mysql", "r2d2", "chrono", "bigdecimal", "numeric"]}
chrono = "0.4"
bigdecimal = "0.0.11"
